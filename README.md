# PrepSuite

Android-first interview practice. Planning and service setup only; no application implementation yet.

## Services

- GitHub: https://github.com/jungwooshim1212/PrepSuite
- Supabase project reference: `qtwhzseowgktmocsjwib`
- Supabase project URL: https://qtwhzseowgktmocsjwib.supabase.co

`supabase/config.toml` is the official CLI-generated local configuration. Its `project_id` identifies the local workspace; it does not authenticate or link the CLI to the hosted project.

The repository integration is configured separately in the Supabase dashboard. This repository contains no database migrations, deployed functions, application credentials, or runtime client connection. Do not deploy or push database changes until an implementation plan is approved.

Keep credentials in approved local secret storage or server-side environment configuration, never in Git. No old SportsSnap application source is included.
